{
  "name": "rtl",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "0.27.2",  
    "bootstrap": "^5.0.1",
    "msw": "^1.2.1",
    "react": "^17.0.2",
    "react-bootstrap": "^1.6.1",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.2.0",
    "react-scripts": "4.0.3",
    "uuid": "^8.3.2",
    "web-vitals": "^1.0.1",
    "@testing-library/react": "^12.1.2", 
    "@testing-library/user-event": "^13.5.0"
  },
  "scripts": {
   "start": "react-scripts --openssl-legacy-provider start",
   "build": "react-scripts --openssl-legacy-provider build",
   "test": "react-scripts test --coverage --collectCoverageFrom='src/components/**/*.js' --watchAll=false",
  "test:debug": "react-scripts test --coverage --verbose",
    "coverage": "react-scripts test --coverage --watchAll=false",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "identity-obj-proxy": "^3.0.0"
  },
  "jest": {
    "transformIgnorePatterns": [
      "node_modules/(?!axios)/"  
    ]
  }
}